export default class ProgressionModelCategory {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
}

export default class ProgressionModelCategoryItem {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
}

